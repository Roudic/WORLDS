{
  "name": "worlds",
  "version": "0.1.0",
  "private": true,
  "description": "Jev-powered model router: classify each request, run it on the cheapest Claude tier that can handle it.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "route": "node cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0"
  }
}
